# Deploy Configuration

This directory contains environment-specific configuration files for SSRS deployment.

## Files

Place environment-specific configuration overrides here if needed.

## Usage

The main deployment configuration is in the root `Config/deployment-config.json` file.
Files in this directory can override those settings for specific deployment scenarios.
